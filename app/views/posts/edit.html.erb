<% if user_signed_in? %>
         <% if current_user.email == "admin@naver.com"%>
<div class="container">
<h1>Editing Post</h1>
<div class="border">
<%= render 'form' %>
</div>
<%= link_to 'Show', @post %> |
<%= link_to 'Back', posts_path %>
</div>
<%else%>
<h3>접근이 불가능한 페이지 입니다.</h3>
<% end%>



<% end%>