<% if user_signed_in? %>
         <% if current_user.email == "admin@naver.com"%>

<div class="container" style="height:800px;">
    <div class="cata_title">
        <h2>메인페이지 관리</h2>
    </div>
<div class="admin_subtitle">
<h3>메인페이지 중앙 큰그림</h3><a href="/home/big_pic_write">새로 등록</a>
</div>
<table class="table">
         <thead>
         <tr class="table_head_row">
         <th class="table_head_num">번호</th>
         <th class="table_head_title">제목</th>
         <th class="table_head_day">등록일</th>
         <th class="table_head_day">삭제</th>
         </tr>
         </thead>
         <tbody>
              <% @bigmain.each do |n| %>
              <tr class="table_con_row">
              <td class=""><%= n.id %></td>
              <td><%= n.title %></td>
              <td class=""><%= n.created_at.in_time_zone("Asia/Shanghai").strftime("%Y-%m-%d") %></td>
              <td><a href="/home/bigmain_destroy/<%=n.id%>"> 삭제</a></td>
              </tr> 
              <% end %>
         </tbody>
     </table>
     <div class="pagination_div"><%= will_paginate @bigmain, :previous_label => t("<<"), :next_label => t(">>")%></div>
<div class="admin_subtitle">
<h3>메인페이지 오른쪽 하단 작은 그림</h3><a href="/home/small_pic_write">새로 등록</a>
</div>
<table class="table">
         <thead>
         <tr class="table_head_row">
         <th class="table_head_num">번호</th>
         <th class="table_head_title">제목</th>
         <th class="table_head_day">등록일</th>
         <th class="table_head_day">삭제</th>
         </tr>
         </thead>
         <tbody>
              <% @small.each do |n| %>
              <tr class="table_con_row">
              <td class=""><%= n.id %></td>
              <td><%= n.title %></td>
              <td class=""><%= n.created_at.in_time_zone("Asia/Shanghai").strftime("%Y-%m-%d") %></td>
              <td><a href="/home/smallmain_destroy/<%=n.id%>"> 삭제</a></td>
              </tr> 
              <% end %>
         </tbody>
     </table>
     <div class="pagination_div"><%= will_paginate @small, :previous_label => t("<<"), :next_label => t(">>")%></div>
</div>

<%else%>
<h3>접근이 불가능한 페이지 입니다.</h3>
<% end%>



<% end%>